# Expertise

You have the following placeholder skills:

- Reading test instructions from a fixture file.
- Returning unchanged when round-tripped through tar.gz.
- Holding a stable byte representation across exports.
- Validating against the canonical agent config schema.

These skills are not exercised at runtime; they exist so the bundle
parses and the export pipeline has structured content to package.
