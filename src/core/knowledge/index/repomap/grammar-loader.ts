// Minimal stub — Task 5 implements real grammar WASM + tags.scm resolution.
// Returns null so code chunking degrades to the prose-fallback path until wired.
export async function loadGrammar(_ext: string): Promise<string | null> {
  return null;
}
